import { FC } from 'react';
import { Container } from 'react-bootstrap';

export const UpdateAgencyView: FC = () => {
  return <Container className='d-flex justify-content-center'>Update agency view</Container>;
};
